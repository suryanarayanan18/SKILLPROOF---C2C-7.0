{
  "id": "interval_consolidation",
  "template_title": "{domain_title}: Interval Consolidation",
  "template_description": "In a {domain_context}, you receive an unordered collection of intervals represented as pairs [start, end]. Consolidate all overlapping or touching intervals into continuous non-overlapping ranges, ordered by start time.",
  "difficulty": "intermediate",
  "concepts": ["intervals", "sorting", "greedy"],
  "algorithm_family": "Intervals & Sorting",
  "constraints": [
    "0 <= len(intervals) <= 50,000",
    "intervals[i][0] <= intervals[i][1]",
    "-1,000,000 <= start, end <= 1,000,000"
  ],
  "starter_code": "from typing import List\n\ndef solve(intervals: List[List[int]]) -> List[List[int]]:\n    \"\"\"Consolidate overlapping intervals into disjoint sorted ranges.\"\"\"\n    # Write your solution here\n    pass\n",
  "reference_solution": "def solve(intervals):\n    if not intervals:\n        return []\n    sorted_intervals = sorted(intervals, key=lambda x: x[0])\n    merged = [list(sorted_intervals[0])]\n    for current in sorted_intervals[1:]:\n        prev = merged[-1]\n        if current[0] <= prev[1]:\n            prev[1] = max(prev[1], current[1])\n        else:\n            merged.append(list(current))\n    return merged\n",
  "version": "1.0",
  "scenarios": [
    {
      "domain_title": "Calendar Scheduling",
      "domain_context": "room reservation scheduling system",
      "metric_name": "meeting block"
    },
    {
      "domain_title": "Disk Range Allocation",
      "domain_context": "storage block partition manager",
      "metric_name": "sector address span"
    },
    {
      "domain_title": "Maintenance Windows",
      "domain_context": "cloud infrastructure maintenance schedule",
      "metric_name": "outage window"
    }
  ],
  "parameter_ranges": {},
  "fixed_tests": [
    {
      "name": "Standard overlapping intervals",
      "input": {"intervals": [[1, 3], [2, 6], [8, 10], [15, 18]]},
      "expected": [[1, 6], [8, 10], [15, 18]]
    },
    {
      "name": "Touching boundary intervals",
      "input": {"intervals": [[1, 4], [4, 5]]},
      "expected": [[1, 5]]
    },
    {
      "name": "Fully enclosed intervals",
      "input": {"intervals": [[1, 10], [2, 3], [4, 8]]},
      "expected": [[1, 10]]
    },
    {
      "name": "Empty input",
      "input": {"intervals": []},
      "expected": []
    },
    {
      "name": "Unsorted disjoint intervals",
      "input": {"intervals": [[10, 12], [1, 2], [5, 7]]},
      "expected": [[1, 2], [5, 7], [10, 12]]
    }
  ]
}
